name: Build Docker Image

on:
  workflow_call:
    # This workflow is designed to be called by other workflows, not triggered directly
    inputs:
      tag:
        description: 'Docker image tag to apply'
        required: true
        type: string
  workflow_dispatch:
    # Keep manual trigger for debugging purposes
    inputs:
      tag:
        description: 'Docker image tag to apply'
        required: true
        type: string

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v5

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        id: docker_build
        uses: docker/build-push-action@v6
        with:
          context: .
          target: production
          tags: oidc-mock:${{ inputs.tag }}
          outputs: type=docker,dest=/tmp/oidc-mock-image.tar
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Upload Docker image artifact
        uses: actions/upload-artifact@v4
        with:
          name: oidc-mock-image
          path: /tmp/oidc-mock-image.tar
          retention-days: 1
